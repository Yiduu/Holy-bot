diff --git a/frontend/admin.html b/frontend/admin.html
index c0942af..13615bf 100644
--- a/frontend/admin.html
+++ b/frontend/admin.html
@@ -900,6 +900,52 @@
             gap: 8px
         }
 
+        .ticket-typing-row-admin {
+            display: flex;
+            align-items: center;
+            gap: 6px;
+            font-size: 11px;
+            color: var(--text-dim);
+            margin-top: 6px;
+        }
+
+        .typing-dots {
+            display: inline-flex;
+            align-items: center;
+            gap: 3px;
+        }
+
+        .typing-dots span {
+            width: 4px;
+            height: 4px;
+            border-radius: 50%;
+            background: var(--text-dim);
+            animation: typing-bounce 1.2s infinite ease-in-out;
+        }
+
+        .typing-dots span:nth-child(2) {
+            animation-delay: 0.15s;
+        }
+
+        .typing-dots span:nth-child(3) {
+            animation-delay: 0.3s;
+        }
+
+        @keyframes typing-bounce {
+
+            0%,
+            60%,
+            100% {
+                transform: translateY(0);
+                opacity: 0.5;
+            }
+
+            30% {
+                transform: translateY(-3px);
+                opacity: 1;
+            }
+        }
+
         /* ── Inline textarea ─────────────────────────────────────── */
         .inline-area {
             padding: 8px 10px;
@@ -2534,18 +2580,63 @@
         }
 
         /* ── Toast ─────────────────────────────────────────────────── */
-        function toast(msg, type = 'info') {
+        function toast(msg, type = 'info', icon = null) {
             const t = document.createElement('div');
             t.className = 'toast';
             const bg = { error: 'var(--danger)', success: 'var(--success)', info: 'var(--bg3)' }[type] || 'var(--bg3)';
             t.style.background = bg;
             t.style.color = type === 'info' ? 'var(--text)' : '#fff';
-            t.textContent = msg;
+            if (icon) {
+                t.style.display = 'flex';
+                t.style.alignItems = 'center';
+                t.style.gap = '8px';
+                t.innerHTML = `${ticketIcon(icon, 16)}<span>${esc(msg)}</span>`;
+            } else {
+                t.textContent = msg;
+            }
             document.body.appendChild(t);
             setTimeout(() => t.style.opacity = '0', 2500);
             setTimeout(() => t.remove(), 2800);
         }
 
+        /* ── Support icon set (mirrors the mini-app's icon set) ───────── */
+        const TICKET_ICONS = {
+            ticket: '<path d="M3 9a2 2 0 0 1 2-2h14a2 2 0 0 1 2 2v1a2 2 0 0 0 0 4v1a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2v-1a2 2 0 0 0 0-4z"/><path d="M9 7v10" stroke-dasharray="2.5 2.5"/>',
+            chat: '<path d="M21 12a8.5 8.5 0 0 1-8.5 8.5 8.4 8.4 0 0 1-3.8-.9L3 21l1.4-4.7A8.4 8.4 0 0 1 3.5 12 8.5 8.5 0 0 1 12 3.5 8.5 8.5 0 0 1 21 12z"/>',
+            user: '<circle cx="12" cy="8" r="4"/><path d="M4 20c0-3.9 3.6-7 8-7s8 3.1 8 7"/>',
+            shield: '<path d="M12 2l8 3.2v6c0 5-3.4 8.7-8 10.8-4.6-2.1-8-5.8-8-10.8v-6z"/><path d="m9 12 2 2 4-4"/>',
+            check: '<circle cx="12" cy="12" r="9"/><path d="m8 12.5 2.5 2.5L16 9.5"/>',
+            reopen: '<path d="M3 12a9 9 0 1 1 3 6.7"/><path d="M3 21v-5h5"/>',
+            clock: '<circle cx="12" cy="12" r="9"/><path d="M12 7v5l3.3 2"/>',
+            bell: '<path d="M6 9a6 6 0 0 1 12 0c0 4.2 1.5 6 2 7H4c.5-1 2-2.8 2-7z"/><path d="M9.5 19a2.5 2.5 0 0 0 5 0"/>',
+            x: '<circle cx="12" cy="12" r="9"/><path d="m9 9 6 6m0-6-6 6"/>'
+        };
+        function ticketIcon(name, size = 15) {
+            const body = TICKET_ICONS[name] || '';
+            return `<svg width="${size}" height="${size}" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" style="flex-shrink:0;vertical-align:-2px">${body}</svg>`;
+        }
+
+        /* ── New-request desktop notification + sound ─────────────────── */
+        if (window.Notification && Notification.permission === 'default') {
+            // Ask once, quietly — first user interaction satisfies the browser gesture
+            // requirement in most browsers; if it's denied we just fall back to toasts.
+            document.addEventListener('click', () => Notification.requestPermission(), { once: true });
+        }
+        const NOTIFY_BEEP = 'data:audio/wav;base64,';
+        function notifyNewTicket(subject) {
+            try {
+                new Audio(NOTIFY_BEEP).play().catch(() => { });
+            } catch (e) { /* audio not supported — ignore */ }
+            if (window.Notification && Notification.permission === 'granted') {
+                try {
+                    new Notification('New support request', {
+                        body: subject || 'A support seeker just opened a new ticket.',
+                        tag: 'holy-support-ticket'
+                    });
+                } catch (e) { /* ignore */ }
+            }
+        }
+
         /* ── Helpers ───────────────────────────────────────────────── */
         function esc(s) { const d = document.createElement('div'); d.textContent = s ?? ''; return d.innerHTML; }
         function ago(d) {
@@ -2629,15 +2720,36 @@
                 wsConn.on('connect_error', () => setWSStatus(false));
 
                 wsConn.on('new_ticket', (d) => {
-                    toast(`🎫 New support request: "${d.subject || 'Untitled'}"`, 'info');
+                    toast(`New support request: "${d.subject || 'Untitled'}"`, 'info', 'ticket');
+                    notifyNewTicket(d.subject);
+                    bumpTicketsBadge(1);
+                    loadStats();
+                    if (activeTab === 'tickets') loadTickets();
+                });
+
+                wsConn.on('new_ticket_reply', (d = {}) => {
+                    loadStats();
+                    if (activeTab === 'tickets') loadTickets();
+                    showTicketTyping(d.ticket_id, false);
+                });
+
+                wsConn.on('ticket_resolved_by_user', (d = {}) => {
+                    toast(`"${d.subject || 'A request'}" was marked solved by the user`, 'success', 'check');
                     loadStats();
                     if (activeTab === 'tickets') loadTickets();
                 });
 
-                wsConn.on('new_ticket_reply', () => {
+                wsConn.on('ticket_reopened_by_user', (d = {}) => {
+                    toast(`"${d.subject || 'A request'}" was reopened by the user`, 'info', 'reopen');
+                    loadStats();
                     if (activeTab === 'tickets') loadTickets();
                 });
 
+                wsConn.on('ticket_typing', ({ ticket_id, sender_type } = {}) => {
+                    if (sender_type !== 'user') return;
+                    showTicketTyping(ticket_id, true);
+                });
+
                 wsConn.on('broadcast', () => toast('Broadcast delivered', 'success'));
 
                 wsConn.on('admin:session_activity', (d) => {
@@ -2647,6 +2759,30 @@
                 });
             } catch { setWSStatus(false); }
         }
+
+        // Optimistic badge bump so the count updates the instant a socket event
+        // lands, without waiting for the next /stats round trip.
+        function bumpTicketsBadge(delta) {
+            const tb = document.getElementById('ticketsBadge');
+            if (!tb) return;
+            const current = parseInt(tb.textContent, 10) || 0;
+            const next = Math.max(0, current + delta);
+            tb.textContent = next;
+            tb.style.display = next > 0 ? 'inline' : 'none';
+        }
+
+        const ticketTypingTimers = {};
+        function showTicketTyping(ticketId, show) {
+            const el = document.getElementById(`typing-${ticketId}`);
+            if (!el) return;
+            clearTimeout(ticketTypingTimers[ticketId]);
+            if (show) {
+                el.style.display = 'flex';
+                ticketTypingTimers[ticketId] = setTimeout(() => { el.style.display = 'none'; }, 3000);
+            } else {
+                el.style.display = 'none';
+            }
+        }
         function setWSStatus(on) {
             document.getElementById('wsDot').className = 'ws-dot' + (on ? '' : ' offline');
             document.getElementById('wsLabel').textContent = on ? 'Live' : 'Offline';
@@ -3025,26 +3161,35 @@
             c.innerHTML = '<div class="loading">Loading…</div>';
             try {
                 const tickets = await api('/api/admin/tickets');
-                if (!tickets?.length) { c.innerHTML = '<div class="empty">🎉 No support requests</div>'; return; }
+                if (!tickets?.length) { c.innerHTML = `<div class="empty">${ticketIcon('check', 20)} No support requests</div>`; return; }
                 c.innerHTML = tickets.map(t => {
                     const statusClass = t.status === 'open' ? 'chip-gold' : t.status === 'in_progress' ? 'chip-blue' : t.status === 'resolved' ? 'chip-green' : 'chip-gray';
                     const replyCount = t.reply_count || 0;
                     const lastReplyTime = t.last_reply_at ? ago(t.last_reply_at) : ago(t.created_at);
+                    const solvedTag = t.resolved_by === 'user'
+                        ? `<span class="chip chip-green" style="display:inline-flex;align-items:center;gap:4px;font-size:11px">${ticketIcon('check', 11)} User confirmed solved</span>`
+                        : '';
 
                     return `
       <div class="ticket-card" id="ticket-${t.id}">
         <div class="ticket-hdr">
           <div>
-            <div class="ticket-subject">${esc(t.subject)}</div>
+            <div class="ticket-subject" style="display:flex;align-items:center;gap:6px">${ticketIcon('ticket', 14)} ${esc(t.subject)}</div>
             <div class="ticket-meta">From ${esc(t.user?.anonymous_id || String(t.telegram_id))} · Submitted ${ago(t.created_at)} · Last active ${lastReplyTime}</div>
           </div>
-          <div style="display:flex;align-items:center;gap:6px">
-            <span class="chip" style="background:var(--bg3);font-size:11px">💬 ${replyCount} ${replyCount === 1 ? 'reply' : 'replies'}</span>
+          <div style="display:flex;align-items:center;gap:6px;flex-wrap:wrap;justify-content:flex-end">
+            ${solvedTag}
+            <span class="chip" style="background:var(--bg3);font-size:11px;display:inline-flex;align-items:center;gap:4px">${ticketIcon('chat', 11)} ${replyCount} ${replyCount === 1 ? 'reply' : 'replies'}</span>
             <span class="chip ${statusClass}">${t.status}</span>
           </div>
         </div>
         <div class="ticket-body">${esc(t.description)}</div>
-        
+
+        <div id="typing-${t.id}" class="ticket-typing-row-admin" style="display:none">
+          ${ticketIcon('user', 12)} <span>User is typing…</span>
+          <span class="typing-dots"><span></span><span></span><span></span></span>
+        </div>
+
         <!-- Toggleable Reply Thread -->
         <div id="thread-container-${t.id}" style="display:none;margin-top:10px;padding-top:10px;border-top:1px dashed var(--border)">
           <div style="font-size:12px;font-weight:600;margin-bottom:6px;color:var(--text-dim)">Reply Thread:</div>
@@ -3054,14 +3199,14 @@
         </div>
 
         <div style="margin-top:8px">
-          <button class="btn btn-ghost btn-sm" style="font-size:11.5px;padding:3px 8px" onclick="toggleTicketThread('${t.id}')" id="btn-thread-${t.id}">📜 View Thread (${replyCount})</button>
+          <button class="btn btn-ghost btn-sm" style="font-size:11.5px;padding:3px 8px;display:inline-flex;align-items:center;gap:5px" onclick="toggleTicketThread('${t.id}')" id="btn-thread-${t.id}">${ticketIcon('chat', 12)} View Thread (${replyCount})</button>
         </div>
 
         <div class="ticket-reply" style="margin-top:10px">
-          <textarea class="inline-area" id="reply-${t.id}" placeholder="Write an admin reply…" rows="2"></textarea>
+          <textarea class="inline-area" id="reply-${t.id}" placeholder="Write an admin reply…" rows="2" oninput="onAdminTicketTyping('${t.id}')"></textarea>
           <div style="display:flex;gap:7px;flex-wrap:wrap">
             <button class="btn btn-ghost btn-sm" onclick="replyTicket('${t.id}','in_progress')">Reply & In Progress</button>
-            <button class="btn btn-success btn-sm" onclick="replyTicket('${t.id}','resolved')">✓ Resolve</button>
+            <button class="btn btn-success btn-sm" style="display:inline-flex;align-items:center;gap:5px" onclick="replyTicket('${t.id}','resolved')">${ticketIcon('check', 12)} Resolve</button>
             <button class="btn btn-ghost btn-sm" style="color:var(--text-dim)" onclick="replyTicket('${t.id}','closed')">Close</button>
           </div>
         </div>
@@ -3070,6 +3215,15 @@
             } catch (e) { c.innerHTML = `<div class="loading">${e.message}</div>`; }
         }
 
+        let adminTicketTypingLastSent = {};
+        function onAdminTicketTyping(ticketId) {
+            if (!wsConn?.connected) return;
+            const now = Date.now();
+            if (now - (adminTicketTypingLastSent[ticketId] || 0) < 2500) return;
+            adminTicketTypingLastSent[ticketId] = now;
+            wsConn.emit('ticket_typing', { ticket_id: ticketId, sender_type: 'admin' });
+        }
+
         async function toggleTicketThread(id) {
             const container = document.getElementById(`thread-container-${id}`);
             const list = document.getElementById(`thread-list-${id}`);
@@ -3078,12 +3232,12 @@
 
             if (container.style.display !== 'none') {
                 container.style.display = 'none';
-                if (btn) btn.textContent = '📜 View Thread';
+                if (btn) btn.innerHTML = `${ticketIcon('chat', 12)} View Thread`;
                 return;
             }
 
             container.style.display = 'block';
-            if (btn) btn.textContent = '🙈 Hide Thread';
+            if (btn) btn.innerHTML = `${ticketIcon('x', 12)} Hide Thread`;
             list.innerHTML = '<div style="font-size:11px;color:var(--text-dim)">Loading thread…</div>';
 
             try {
@@ -3094,10 +3248,13 @@
                 }
                 list.innerHTML = replies.map(r => {
                     const isAdmin = r.sender_type === 'admin';
+                    const label = isAdmin
+                        ? `${ticketIcon('shield', 11)} Admin`
+                        : `${ticketIcon('user', 11)} User`;
                     return `
                     <div style="padding:6px 9px;border-radius:6px;margin-bottom:6px;background:${isAdmin ? 'var(--bg3)' : 'var(--bg2)'};border-left:3px solid ${isAdmin ? 'var(--accent)' : 'var(--gold)'}">
                       <div style="display:flex;justify-content:space-between;font-size:11px;font-weight:bold;margin-bottom:3px">
-                        <span style="color:${isAdmin ? 'var(--accent)' : 'var(--gold)'}">${isAdmin ? '🛡️ Admin' : '👤 User'}</span>
+                        <span style="color:${isAdmin ? 'var(--accent)' : 'var(--gold)'};display:inline-flex;align-items:center;gap:4px">${label}</span>
                         <span style="color:var(--text-dim);font-weight:normal">${ago(r.created_at)}</span>
                       </div>
                       <div style="font-size:12px;white-space:pre-wrap;color:var(--text)">${esc(r.content)}</div>
@@ -3121,7 +3278,7 @@
                 await api(`/api/admin/tickets/${id}`, { method: 'PATCH', body: { reply, admin_reply: reply, status } });
                 toast('Ticket updated', 'success');
                 if (replyInput) replyInput.value = '';
-                
+
                 const threadContainer = document.getElementById(`thread-container-${id}`);
                 if (threadContainer && threadContainer.style.display !== 'none') {
                     toggleTicketThread(id);
diff --git a/frontend/app.js b/frontend/app.js
index c86d802..84a07fc 100644
--- a/frontend/app.js
+++ b/frontend/app.js
@@ -164,6 +164,27 @@ function escapeHtml(str) {
   d.textContent = str || '';
   return d.innerHTML;
 }
+
+// ─── Support / Ticket Icon Set ──────────────────────────────────────────────
+// Stroke-based, single-color SVGs (inherit currentColor) so they theme with
+// the rest of the app. Used in place of emoji throughout the support system.
+const TICKET_ICONS = {
+  ticket: '<path d="M3 9a2 2 0 0 1 2-2h14a2 2 0 0 1 2 2v1a2 2 0 0 0 0 4v1a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2v-1a2 2 0 0 0 0-4z"/><path d="M9 7v10" stroke-dasharray="2.5 2.5"/>',
+  chat: '<path d="M21 12a8.5 8.5 0 0 1-8.5 8.5 8.4 8.4 0 0 1-3.8-.9L3 21l1.4-4.7A8.4 8.4 0 0 1 3.5 12 8.5 8.5 0 0 1 12 3.5 8.5 8.5 0 0 1 21 12z"/>',
+  calendar: '<rect x="3" y="4.5" width="18" height="16" rx="2"/><path d="M16 2.5v4M8 2.5v4M3 9.5h18"/>',
+  send: '<path d="M22 2 11 13"/><path d="M22 2 15 22l-4-9-9-4z"/>',
+  user: '<circle cx="12" cy="8" r="4"/><path d="M4 20c0-3.9 3.6-7 8-7s8 3.1 8 7"/>',
+  shield: '<path d="M12 2l8 3.2v6c0 5-3.4 8.7-8 10.8-4.6-2.1-8-5.8-8-10.8v-6z"/><path d="m9 12 2 2 4-4"/>',
+  check: '<circle cx="12" cy="12" r="9"/><path d="m8 12.5 2.5 2.5L16 9.5"/>',
+  reopen: '<path d="M3 12a9 9 0 1 1 3 6.7"/><path d="M3 21v-5h5"/>',
+  clock: '<circle cx="12" cy="12" r="9"/><path d="M12 7v5l3.3 2"/>',
+  bell: '<path d="M6 9a6 6 0 0 1 12 0c0 4.2 1.5 6 2 7H4c.5-1 2-2.8 2-7z"/><path d="M9.5 19a2.5 2.5 0 0 0 5 0"/>',
+  inbox: '<path d="M3 12h4l2 3h6l2-3h4"/><path d="M5 5h14l2 7v7a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2v-7z"/>'
+};
+function ticketIcon(name, size = 15) {
+  const body = TICKET_ICONS[name] || '';
+  return `<svg class="ticket-icon" width="${size}" height="${size}" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">${body}</svg>`;
+}
 // ─── Voice / File Attachment Rendering ─────────────────────────────────────
 
 function formatDuration(seconds) {
@@ -1164,17 +1185,29 @@ function connectSocket() {
   socket.on('ticket_reply', (data) => {
     haptic('success');
     const msg = data.reply
-      ? `📩 Admin replied to your support request: "${data.subject || 'Support'}"`
-      : `🔔 Your support request "${data.subject || 'Support'}" is now ${data.status_label || data.status}`;
+      ? `Admin replied to your support request: "${data.subject || 'Support'}"`
+      : `Your support request "${data.subject || 'Support'}" is now ${data.status_label || data.status}`;
     showToast(msg, 'info');
     updateSupportBadge();
     if (currentPage === 'support') {
       loadUserTickets();
     } else if (currentPage === 'ticket-detail' && window.activeTicketId === data.ticket_id) {
       loadTicketDetail(data.ticket_id);
+      hideTicketTyping();
     }
   });
 
+  // Support chat typing indicator (admin → user direction only here)
+  socket.on('ticket_typing', ({ ticket_id, sender_type } = {}) => {
+    if (sender_type !== 'admin') return;
+    if (currentPage !== 'ticket-detail' || String(window.activeTicketId) !== String(ticket_id)) return;
+    const el = $('ticketTypingIndicator');
+    if (!el) return;
+    el.style.display = 'flex';
+    clearTimeout(window.ticketTypingTimeout);
+    window.ticketTypingTimeout = setTimeout(hideTicketTyping, 3000);
+  });
+
   // Fired when a request is accepted or rejected — from the mini app OR the bot
   socket.on('mentorship_request_updated', ({ requestId, status } = {}) => {
     updateRequestsBadge();
@@ -3428,7 +3461,7 @@ async function loadUserTickets() {
     if (!tickets || tickets.length === 0) {
       container.innerHTML = `
         <div class="empty-state card" style="text-align:center;padding:40px 20px;border-radius:18px;background:linear-gradient(135deg, rgba(var(--bg3-rgb),0.5), rgba(var(--bg2-rgb),0.7));border:1px dashed var(--border)">
-          <div style="font-size:2.6rem;margin-bottom:10px">🎫</div>
+          <div style="color:var(--gold-light);margin-bottom:10px">${ticketIcon('ticket', 42)}</div>
           <div class="font-bold text-base mb-4" style="color:var(--text)">${t('no_support_requests_found')}</div>
           <p class="text-xs text-dim mb-16" style="max-width:260px;margin-left:auto;margin-right:auto">${t('no_support_requests_desc')}</p>
           <button class="ticket-submit-btn" style="max-width:180px;margin:0 auto" onclick="toggleNewTicketModal(true)">
@@ -3442,7 +3475,12 @@ async function loadUserTickets() {
       const status = t.status || 'open';
       const replyCount = t.reply_count || 0;
       const previewText = t.last_reply_preview ? escapeHtml(t.last_reply_preview) : escapeHtml(t.description);
-      const lastSenderLabel = t.last_reply_sender === 'admin' ? '🛡️ Admin:' : (t.last_reply_sender === 'user' ? ' You:' : '');
+      const lastSenderLabel = t.last_reply_sender === 'admin'
+        ? `${ticketIcon('shield', 12)} Admin:`
+        : (t.last_reply_sender === 'user' ? 'You:' : '');
+      const resolvedTag = t.resolved_by === 'user'
+        ? `<span class="chip chip-solved">${ticketIcon('check', 11)} You marked solved</span>`
+        : '';
 
       return `
         <div class="ticket-card-premium status-${status}" onclick="openTicketDetail('${t.id}')">
@@ -3454,11 +3492,14 @@ async function loadUserTickets() {
             </span>
           </div>
           <p class="text-xs text-dim mb-12" style="display:-webkit-box;-webkit-line-clamp:2;-webkit-box-orient:vertical;overflow:hidden;line-height:1.45">
-            <strong style="color:var(--text2)">${lastSenderLabel}</strong> ${previewText}
+            <strong style="color:var(--text2);display:inline-flex;align-items:center;gap:3px">${lastSenderLabel}</strong> ${previewText}
           </p>
-          <div class="flex justify-between items-center text-xs text-dim pt-8" style="border-top:1px solid rgba(255,255,255,0.06)">
-            <span style="font-size:0.75rem">📅 Submitted ${timeAgo(t.created_at)}</span>
-            <span class="chip" style="background:rgba(201,168,76,0.1);color:var(--gold-light);font-size:0.72rem;padding:2px 8px;border-radius:12px;border:1px solid rgba(201,168,76,0.2)">💬 ${replyCount} ${replyCount === 1 ? 'reply' : 'replies'}</span>
+          <div class="flex justify-between items-center text-xs text-dim pt-8" style="border-top:1px solid rgba(255,255,255,0.06);gap:8px">
+            <span style="font-size:0.75rem;display:inline-flex;align-items:center;gap:4px">${ticketIcon('calendar', 12)} Submitted ${timeAgo(t.created_at)}</span>
+            <span style="display:flex;align-items:center;gap:6px">
+              ${resolvedTag}
+              <span class="chip" style="background:rgba(201,168,76,0.1);color:var(--gold-light);font-size:0.72rem;padding:2px 8px;border-radius:12px;border:1px solid rgba(201,168,76,0.2);display:inline-flex;align-items:center;gap:4px">${ticketIcon('chat', 11)} ${replyCount} ${replyCount === 1 ? 'reply' : 'replies'}</span>
+            </span>
           </div>
         </div>`;
     }).join('');
@@ -3517,6 +3558,8 @@ async function loadTicketDetail(ticketId) {
     statusEl.className = `status-pill status-pill-${status}`;
     statusEl.innerHTML = `<span class="status-dot"></span>${status.replace('_', ' ')}`;
 
+    window.activeTicketStatus = status;
+
     // Handle closed state input locking
     const replyTextarea = $('userTicketReplyText');
     const replyBtn = $('sendTicketReplyBtn');
@@ -3533,11 +3576,13 @@ async function loadTicketDetail(ticketId) {
       replyBtn.style.opacity = '1';
     }
 
+    renderTicketResolveBar(status, ticket.resolved_by);
+
     // Render original ticket message + reply thread
     let html = `
       <div class="ticket-bubble ticket-bubble-user" style="align-self:flex-start;width:100%;max-width:100%;margin-bottom:12px;background:linear-gradient(135deg, rgba(201,168,76,0.12) 0%, rgba(var(--bg2-rgb),0.85) 100%);border:1px solid rgba(201,168,76,0.25)">
         <div class="ticket-bubble-hdr">
-          <span style="display:flex;align-items:center;gap:4px;color:var(--gold-light)">👤 <strong>You</strong> (Original Issue)</span>
+          <span style="display:flex;align-items:center;gap:5px;color:var(--gold-light)">${ticketIcon('user', 13)} <strong>You</strong> (Original Issue)</span>
           <span style="font-weight:normal;color:var(--text3);font-size:0.7rem">${formatDateTime(ticket.created_at)}</span>
         </div>
         <div class="ticket-bubble-body">${escapeHtml(ticket.description)}</div>
@@ -3547,7 +3592,7 @@ async function loadTicketDetail(ticketId) {
       html += `
         <div class="ticket-bubble ticket-bubble-admin" style="align-self:flex-start;width:100%;max-width:100%;margin-bottom:12px">
           <div class="ticket-bubble-hdr">
-            <span style="display:flex;align-items:center;gap:4px;color:#7AA5FF">🛡️ <strong>Support Admin</strong></span>
+            <span style="display:flex;align-items:center;gap:5px;color:#7AA5FF">${ticketIcon('shield', 13)} <strong>Support Admin</strong></span>
             <span style="font-weight:normal;color:var(--text3);font-size:0.7rem">${formatDateTime(ticket.updated_at || ticket.created_at)}</span>
           </div>
           <div class="ticket-bubble-body">${escapeHtml(ticket.admin_reply)}</div>
@@ -3557,12 +3602,14 @@ async function loadTicketDetail(ticketId) {
     replies.forEach(r => {
       const isAdmin = r.sender_type === 'admin';
       const bubbleClass = isAdmin ? 'ticket-bubble-admin' : 'ticket-bubble-user';
-      const titleText = isAdmin ? '🛡️ Support Admin' : '👤 You';
+      const titleText = isAdmin
+        ? `${ticketIcon('shield', 13)} Support Admin`
+        : `${ticketIcon('user', 13)} You`;
 
       html += `
         <div class="ticket-bubble ${bubbleClass}" style="margin-bottom:8px">
           <div class="ticket-bubble-hdr">
-            <span>${titleText}</span>
+            <span style="display:flex;align-items:center;gap:5px">${titleText}</span>
             <span style="font-weight:normal;color:var(--text3);font-size:0.7rem">${formatDateTime(r.created_at)}</span>
           </div>
           <div class="ticket-bubble-body">${escapeHtml(r.content)}</div>
@@ -3576,6 +3623,62 @@ async function loadTicketDetail(ticketId) {
   }
 }
 
+// Shows "Mark as solved" for an open/in-progress ticket, or a confirmation +
+// reopen option once it's resolved. Hidden entirely once an admin closes it.
+function renderTicketResolveBar(status, resolvedBy) {
+  const bar = $('ticketResolveBar');
+  if (!bar) return;
+
+  if (status === 'closed') {
+    bar.style.display = 'none';
+    return;
+  }
+
+  bar.style.display = 'flex';
+
+  if (status === 'resolved') {
+    const byUser = resolvedBy === 'user';
+    bar.innerHTML = `
+      <span class="ticket-resolve-note">${ticketIcon('check', 15)} ${byUser ? 'You marked this solved' : 'Support marked this resolved'}</span>
+      <button class="btn btn-ghost btn-sm ticket-reopen-btn" onclick="toggleTicketResolved(false)">${ticketIcon('reopen', 13)} Still need help?</button>`;
+  } else {
+    bar.innerHTML = `
+      <span class="ticket-resolve-note">${ticketIcon('clock', 15)} Still open</span>
+      <button class="btn btn-sm ticket-resolve-btn" onclick="toggleTicketResolved(true)">${ticketIcon('check', 13)} Mark as solved</button>`;
+  }
+}
+
+let ticketTypingLastSent = 0;
+function onTicketReplyTyping() {
+  if (!window.activeTicketId || !socket?.connected) return;
+  const now = Date.now();
+  if (now - ticketTypingLastSent < 2500) return;
+  ticketTypingLastSent = now;
+  socket.emit('ticket_typing', { ticket_id: window.activeTicketId, sender_type: 'user' });
+}
+
+function hideTicketTyping() {
+  const el = $('ticketTypingIndicator');
+  if (el) el.style.display = 'none';
+}
+
+async function toggleTicketResolved(resolved) {
+  if (!window.activeTicketId) return;
+  haptic(resolved ? 'success' : 'medium');
+  try {
+    await apiFetch(`/api/support/${window.activeTicketId}/resolve`, {
+      method: 'PATCH',
+      body: { resolved }
+    });
+    showToast(resolved ? 'Marked as solved — thank you!' : 'Reopened. We\u2019ll take another look.', 'success');
+    loadTicketDetail(window.activeTicketId);
+    updateSupportBadge();
+  } catch (e) {
+    haptic('error');
+    showToast(e.message, 'error');
+  }
+}
+
 async function submitTicketReply() {
   if (!window.activeTicketId) return;
   const replyInput = $('userTicketReplyText');
@@ -3595,6 +3698,7 @@ async function submitTicketReply() {
     haptic('success');
     showToast('Reply sent', 'success');
     replyInput.value = '';
+    updateSupportBadge();
     loadTicketDetail(window.activeTicketId);
   } catch (e) {
     haptic('error');
@@ -3624,7 +3728,7 @@ async function submitModalTicket() {
   try {
     await apiFetch('/api/support', { method: 'POST', body: { subject, description, category } });
     haptic('success');
-    showToast('Support request submitted 🙏', 'success');
+    showToast('Support request submitted', 'success');
     $('modalTicketSubject').value = '';
     $('modalTicketDesc').value = '';
     if ($('modalTicketCategory')) $('modalTicketCategory').value = '';
diff --git a/frontend/index.html b/frontend/index.html
index b21effc..6080f8a 100644
--- a/frontend/index.html
+++ b/frontend/index.html
@@ -997,14 +997,23 @@
           <div class="loading-spinner" style="margin:20px auto"></div>
         </div>
 
+        <div id="ticketTypingIndicator" class="ticket-typing-row" style="display:none">
+          <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M12 2l8 3.2v6c0 5-3.4 8.7-8 10.8-4.6-2.1-8-5.8-8-10.8v-6z"/><path d="m9 12 2 2 4-4"/></svg>
+          <span>Support is typing</span>
+          <span class="typing-dots"><span></span><span></span><span></span></span>
+        </div>
+
+        <div id="ticketResolveBar" class="ticket-resolve-bar" style="display:none"></div>
+
         <div id="ticketReplyBox" class="ticket-reply-panel">
           <div class="form-group mb-4">
             <textarea id="userTicketReplyText" class="ticket-reply-textarea" rows="3"
-              placeholder="Write a follow-up message..." data-i18n="ticket_reply_placeholder"></textarea>
+              placeholder="Write a follow-up message..." data-i18n="ticket_reply_placeholder"
+              oninput="onTicketReplyTyping()"></textarea>
           </div>
           <button id="sendTicketReplyBtn" class="ticket-submit-btn" onclick="submitTicketReply()">
             <span>Send Reply</span>
-            <span>💬</span>
+            <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M22 2 11 13"/><path d="M22 2 15 22l-4-9-9-4z"/></svg>
           </button>
         </div>
       </div>
@@ -1120,7 +1129,7 @@
         <button class="btn btn-ghost flex-1" onclick="toggleNewTicketModal(false)" data-i18n="btn_cancel">Cancel</button>
         <button class="ticket-submit-btn flex-1" style="margin-top:0" onclick="submitModalTicket()">
           <span data-i18n="btn_submit_request">Submit Request</span>
-          <span>🙏</span>
+          <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M22 2 11 13"/><path d="M22 2 15 22l-4-9-9-4z"/></svg>
         </button>
       </div>
     </div>
diff --git a/frontend/styles.css b/frontend/styles.css
index 4c04b76..d5242b0 100644
--- a/frontend/styles.css
+++ b/frontend/styles.css
@@ -4116,6 +4116,74 @@ textarea.form-control {
   cursor: not-allowed;
   box-shadow: none;
 }
+
+/* ── Support icon set ─────────────────────────────────────────── */
+.ticket-icon {
+  flex-shrink: 0;
+  vertical-align: -2px;
+}
+
+.chip-solved {
+  display: inline-flex;
+  align-items: center;
+  gap: 4px;
+  background: rgba(91, 173, 122, 0.14);
+  color: #72C793;
+  border: 1px solid rgba(91, 173, 122, 0.35);
+  font-size: 0.68rem;
+  font-weight: 700;
+  padding: 2px 8px;
+  border-radius: 12px;
+}
+
+/* ── Resolve / reopen bar in ticket chat ──────────────────────── */
+.ticket-resolve-bar {
+  align-items: center;
+  justify-content: space-between;
+  gap: 10px;
+  padding: 10px 14px;
+  margin-bottom: 10px;
+  border-radius: 12px;
+  background: rgba(var(--bg3-rgb), 0.55);
+  border: 1px solid rgba(255, 255, 255, 0.08);
+}
+
+.ticket-resolve-note {
+  display: inline-flex;
+  align-items: center;
+  gap: 6px;
+  font-size: 0.8rem;
+  font-weight: 600;
+  color: var(--text2);
+}
+
+.ticket-resolve-btn {
+  display: inline-flex;
+  align-items: center;
+  gap: 5px;
+  background: linear-gradient(135deg, #5BAD7A 0%, #3F8A5D 100%);
+  color: #fff;
+  border: none;
+  font-weight: 700;
+  border-radius: 10px;
+  padding: 6px 12px;
+}
+
+.ticket-reopen-btn {
+  display: inline-flex;
+  align-items: center;
+  gap: 5px;
+}
+
+/* ── Ticket chat typing indicator ─────────────────────────────── */
+.ticket-typing-row {
+  display: flex;
+  align-items: center;
+  gap: 6px;
+  font-size: 0.75rem;
+  color: var(--text3);
+  padding: 0 4px 8px;
+}
 /* ============================================================
    Organic UI Enhancements — Blobs, Stagger, Mood Picker, Ripple
    Additive layer only: no existing rule above is modified,
diff --git a/migrations/20260810_add_resolved_by_to_support_tickets.sql b/migrations/20260810_add_resolved_by_to_support_tickets.sql
new file mode 100644
index 0000000..bcb0420
--- /dev/null
+++ b/migrations/20260810_add_resolved_by_to_support_tickets.sql
@@ -0,0 +1,6 @@
+BEGIN;
+-- Tracks who marked the ticket resolved: 'user' (support seeker confirmed their
+-- issue is solved) or 'admin' (support team closed it out). NULL = not resolved.
+ALTER TABLE support_tickets ADD COLUMN IF NOT EXISTS resolved_by TEXT
+  CHECK (resolved_by IN ('user', 'admin'));
+COMMIT;
diff --git a/routes/admin.js b/routes/admin.js
index f4c9d94..10b0250 100644
--- a/routes/admin.js
+++ b/routes/admin.js
@@ -79,7 +79,7 @@ module.exports = function adminRoutes(supabase, requireAuth, requireAdmin, io) {
       supabase.from('users').select('telegram_id', { count: 'exact', head: true }).eq('role', 'mentor'),
       supabase.from('mentor_applications').select('id', { count: 'exact', head: true }).eq('status', 'pending'),
       supabase.from('messages').select('id', { count: 'exact', head: true }).eq('is_flagged', true),
-      supabase.from('support_tickets').select('id', { count: 'exact', head: true }).eq('status', 'open'),
+      supabase.from('support_tickets').select('id', { count: 'exact', head: true }).in('status', ['open', 'in_progress']),
     ]);
 
     res.json({
@@ -641,11 +641,17 @@ module.exports = function adminRoutes(supabase, requireAuth, requireAdmin, io) {
       newReplyCount += 1;
     }
 
+    const isResolvedOrClosed = updatedStatus === 'resolved' || updatedStatus === 'closed';
+    const resolvedBy = statusChanged
+      ? (isResolvedOrClosed ? 'admin' : null)
+      : ticket.resolved_by;
+
     const { data, error } = await supabase
       .from('support_tickets')
       .update({
         admin_reply: replyText || ticket.admin_reply,
         status: updatedStatus,
+        resolved_by: resolvedBy,
         last_reply_at: (replyText || statusChanged) ? now : (ticket.last_reply_at || now),
         reply_count: newReplyCount,
         updated_at: now
@@ -658,6 +664,18 @@ module.exports = function adminRoutes(supabase, requireAuth, requireAdmin, io) {
 
     await logAudit(admin_id, 'ticket_reply', null, 'support_ticket', { ticket_id: req.params.id, status: updatedStatus });
 
+    // Let every admin dashboard (not just this one) refresh its ticket list
+    // and open-ticket badge in real time.
+    if (io) {
+      io.emit('new_ticket_reply', {
+        ticket_id: ticket.id,
+        subject: ticket.subject,
+        sender_type: 'admin',
+        status: updatedStatus,
+        created_at: now
+      });
+    }
+
     // Notify the user on any reply OR status change — not just when text was typed
     if (replyText || statusChanged) {
       const STATUS_LABEL = { open: 'Open', in_progress: 'In Progress', resolved: 'Resolved', closed: 'Closed' };
diff --git a/routes/support.js b/routes/support.js
index 17f1c9c..7e12f8b 100644
--- a/routes/support.js
+++ b/routes/support.js
@@ -21,6 +21,7 @@ module.exports = function supportRoutes(supabase, requireAuth, io, onlineUsers)
         category: category || null,
         status: 'open',
         reply_count: 0,
+        resolved_by: null,
         last_reply_at: now,
         created_at: now,
         updated_at: now
@@ -32,7 +33,7 @@ module.exports = function supportRoutes(supabase, requireAuth, io, onlineUsers)
 
     // Notify admins via socket
     if (io) {
-      io.emit('new_ticket', { id: data.id, subject: data.subject, telegram_id });
+      io.emit('new_ticket', { id: data.id, subject: data.subject, telegram_id, created_at: now });
     }
 
     res.status(201).json(data);
@@ -166,6 +167,8 @@ module.exports = function supportRoutes(supabase, requireAuth, io, onlineUsers)
         last_reply_at: now,
         reply_count: newReplyCount,
         status: newStatus,
+        // A fresh message means the issue isn't settled anymore
+        resolved_by: newStatus === 'open' ? null : ticket.resolved_by,
         updated_at: now
       })
       .eq('id', ticketId);
@@ -174,6 +177,7 @@ module.exports = function supportRoutes(supabase, requireAuth, io, onlineUsers)
     if (io) {
       io.emit('new_ticket_reply', {
         ticket_id: ticketId,
+        subject: ticket.subject,
         sender_type: 'user',
         sender_id: telegram_id,
         content: content.trim(),
@@ -184,5 +188,56 @@ module.exports = function supportRoutes(supabase, requireAuth, io, onlineUsers)
     res.status(201).json(reply);
   });
 
+  // PATCH /api/support/:id/resolve – support seeker marks their own ticket
+  // resolved (issue solved) or reopens it (still needs help). Admin-only
+  // "closed" status is untouched here — this is the user's own confirmation.
+  router.patch('/:id/resolve', requireAuth, async (req, res) => {
+    const { id: telegram_id } = req.telegramUser;
+    const ticketId = req.params.id;
+    const resolved = !!req.body.resolved;
+
+    const { data: ticket, error: ticketErr } = await supabase
+      .from('support_tickets')
+      .select('*')
+      .eq('id', ticketId)
+      .single();
+
+    if (ticketErr || !ticket) return res.status(404).json({ error: 'Ticket not found' });
+    if (String(ticket.telegram_id) !== String(telegram_id)) {
+      return res.status(403).json({ error: 'Forbidden' });
+    }
+    if (ticket.status === 'closed') {
+      return res.status(400).json({ error: 'This request has already been closed' });
+    }
+
+    const now = new Date().toISOString();
+    const newStatus = resolved ? 'resolved' : 'open';
+
+    const { data, error } = await supabase
+      .from('support_tickets')
+      .update({
+        status: newStatus,
+        resolved_by: resolved ? 'user' : null,
+        last_reply_at: now,
+        updated_at: now
+      })
+      .eq('id', ticketId)
+      .select()
+      .single();
+
+    if (error) return res.status(500).json({ error: error.message });
+
+    if (io) {
+      io.emit(resolved ? 'ticket_resolved_by_user' : 'ticket_reopened_by_user', {
+        ticket_id: ticketId,
+        subject: ticket.subject,
+        telegram_id,
+        created_at: now
+      });
+    }
+
+    res.json(data);
+  });
+
   return router;
 };
diff --git a/server.js b/server.js
index 87a4807..92fb687 100644
--- a/server.js
+++ b/server.js
@@ -113,6 +113,13 @@ io.on('connection', (socket) => {
     if (targetSocket) io.to(targetSocket).emit('typing', { from_id: socket.data.telegram_id });
   });
 
+  // Support ticket typing indicator — broadcast to everyone else (the ticket
+  // owner + every admin dashboard). Cheap and stateless by design.
+  socket.on('ticket_typing', ({ ticket_id, sender_type }) => {
+    if (!ticket_id || (sender_type !== 'user' && sender_type !== 'admin')) return;
+    socket.broadcast.emit('ticket_typing', { ticket_id, sender_type });
+  });
+
   socket.on('disconnect', () => {
     if (socket.data.telegram_id) {
       onlineUsers.delete(socket.data.telegram_id);
